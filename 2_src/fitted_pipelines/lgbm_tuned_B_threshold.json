{
  "set": "B",
  "model": "LightGBM",
  "threshold": 0.594,
  "calibrated_on": "train-side TimeSeriesSplit CV validation folds only",
  "note": "Use pipe.predict_proba(X)[:, 1] >= threshold at inference time, NOT pipe.predict(X) (which hard-codes 0.5)."
}